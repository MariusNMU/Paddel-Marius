

# Configuration JWT locale de démonstration.
# La valeur par défaut est réservée au démarrage local.
# En déploiement réel, définir PADEL_JWT_SECRET avec un secret externe robuste.
padel.jwt.secret=${PADEL_JWT_SECRET:padel-marius-dev-secret-change-me-2026}
padel.jwt.expiration-minutes=60
padel.jwt.refresh-expiration-days=7

# En local HTTP, le cookie Secure doit rester désactivé.
# En déploiement HTTPS, définir PADEL_REFRESH_COOKIE_SECURE=true.
padel.jwt.refresh-cookie-secure=${PADEL_REFRESH_COOKIE_SECURE:false}

# Le cours recommande de protéger le login par une limite de tentatives.
# La limite est appliquée par adresse cliente et par endpoint d'authentification.
padel.security.auth-rate-limit.max-attempts=${PADEL_AUTH_MAX_ATTEMPTS:5}
padel.security.auth-rate-limit.window-minutes=${PADEL_AUTH_WINDOW_MINUTES:10}

# Le health check standard est fourni par Spring Boot Actuator.
# Aucun autre endpoint Actuator n'est exposé sur HTTP.
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never
management.endpoints.web.discovery.enabled=false
