spring:
  application:
    name: padel-backend

  datasource:
    url: jdbc:h2:mem:padeldb
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080